name: Project build
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Install 🔧
        run: npm install
        
      - name: Run Test 🔨
        run: npm run test

      - name: Build 👷
        run: npm run build
        
      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@3.7.1
        with:
          GITHUB_TOKEN: ${{ secrets.KEY_FOR_ACTION }}
          BRANCH: gh-pages
          FOLDER: public
          CLEAN: true
